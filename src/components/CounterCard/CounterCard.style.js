import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    padding: 5,
    flexDirection:'row',
  },
  innerContainer: {
      flex:1,
  },
  text: {
    color: "#FFA500",
    fontSize: 40,
  },
});
